spring:
  application:
    name: tournament-api-service

  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  liquibase:
    enabled: true

server:
  port: 8080