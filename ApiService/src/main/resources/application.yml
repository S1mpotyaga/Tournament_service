spring:
  application:
    name: tournament-api-service

  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true


  liquibase:
    enabled: true

server:
  port: 8080